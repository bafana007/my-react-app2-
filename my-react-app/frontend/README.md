# Frontend

Deploy this folder as its own Render Static Site.

Render settings:
- Root Directory: frontend  (or leave blank if this is the repo root)
- Build Command: npm install && npm run build
- Publish Directory: dist
