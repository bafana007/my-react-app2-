# Backend

Deploy this folder as its own Render Web Service.

Render settings:
- Root Directory: backend  (or leave blank if this is the repo root)
- Build Command: npm install
- Start Command: npm start
